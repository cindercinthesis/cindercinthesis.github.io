---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
---

<Layout title="I often wonder what it means to exist" description="Oh to exist!">
  <main class="home-shell">
    <Header showSocials={false} />

    <section class="home-hero">
      <p class="eyebrow">Hello</p>
      <h2>I often wonder what it means to exist.</h2>
      <p class="lede">
        You’re at the front door. Choose a path and linger as long as you like.
      </p>
      <div class="cta-grid">
        <a class="card" href="/photography/">
          <span class="label">Photography</span>
          <!-- <span class="hint">See the images</span> -->
        </a>
        <a class="card" href="/writing/">
          <span class="label">Writing</span>
          <!-- <span class="hint">Read the words</span> -->
        </a>
        <a class="card" href="/music/">
          <span class="label">Music</span>
          <!-- <span class="hint">Hear the sound</span> -->
        </a>
      </div>
    </section>
  </main>
</Layout>

<style>
  :global(body) {
    background: radial-gradient(circle at 10% 20%, #f9f1e6 0%, #f5ecdf 35%, #f2e7d7 60%, #efe2cf 100%);
    color: #1c1a17;
    font-family: "Cormorant Garamond", "Iowan Old Style", Georgia, serif;
    letter-spacing: 0.01em;
  }

  .home-shell {
    max-width: 960px;
    margin: 0 auto;
    padding: 3rem 1.5rem 4.5rem;
  }

  .home-hero {
    margin-top: 2rem;
    display: grid;
    gap: 1.25rem;
  }

  .eyebrow {
    letter-spacing: 0.14em;
    text-transform: uppercase;
    font-size: 0.85rem;
    color: #7b6a55;
    margin: 0;
  }

  .home-hero h2 {
    font-size: clamp(2.5rem, 6vw, 3.6rem);
    line-height: 1.05;
    margin: 0;
    color: #1c1a17;
  }

  .lede {
    font-family: "Neue Haas Grotesk", "Helvetica Neue", Arial, sans-serif;
    font-size: 1.05rem;
    line-height: 1.8;
    max-width: 640px;
    color: #3a362f;
    margin: 0;
  }

  .cta-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 0.9rem;
    margin-top: 1rem;
  }

  .card {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    padding: 1.35rem 1.5rem;
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.6);
    border: 1px solid rgba(28, 26, 23, 0.08);
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.04);
    color: inherit;
    text-decoration: none;
    transition: transform 160ms ease, box-shadow 160ms ease, border-color 160ms ease, background 160ms ease;
  }

  .card:hover,
  .card:focus-visible {
    transform: translateY(-6px);
    border-color: rgba(28, 26, 23, 0.18);
    background: rgba(255, 255, 255, 0.9);
    box-shadow: 0 22px 45px rgba(0, 0, 0, 0.07);
    outline: none;
  }

  .label {
    font-size: 1.2rem;
    font-weight: 600;
    color: #1c1a17;
  }

  .hint {
    font-family: "Neue Haas Grotesk", "Helvetica Neue", Arial, sans-serif;
    font-size: 0.95rem;
    color: #4f4a42;
  }

  @media (max-width: 640px) {
    .home-shell {
      padding: 2.25rem 1.2rem 3.5rem;
    }

    .home-hero {
      margin-top: 1.4rem;
    }
  }
</style>
